import React from "react"
import PropTypes from "prop-types"
class Crm extends React.Component {
  render () {
    return (
      <React.Fragment>
      </React.Fragment>
    );
  }
}

export default Crm
